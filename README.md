Insight Data Engineering - Coding Challenge
===========================================================

Test suite depends rspec gem. Gems for this project managed by bundler. Before running tests
first install bundler

		gem install bundler

Then install the gems with the following command:

		bundle install

Existing test shell script will call rspec tests as well. 
